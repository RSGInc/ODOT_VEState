# When starting R from this directory, load the build helpers
source("build/VisionEval-dev.R")
sys_path <- Sys.getenv("PATH")
sys_path <- paste0("C:\\rtools40\\usr\\bin;",sys_path)
Sys.setenv(PATH=sys_path)
rm(sys_path)
